server:
  port: 20020
  servlet:
    context-path: /api/v1

spring:
  jpa:
    database: postgresql
    show-sql: false
    hibernate:
      ddl-auto: update
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/clientedb
    username: postgres
    password: admin